services:
  mellow-finance:
    build: .
    container_name: mellow-finance
    ports:
      - "8000:8000"
    volumes:
      - ./data:/data
    environment:
      - DATA_DIR=/data
    restart: unless-stopped